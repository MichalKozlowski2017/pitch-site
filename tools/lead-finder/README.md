# Lead finder — ocena ogłoszeń

CLI do scoringu leadów. Pełna dokumentacja: [`docs/lead-scoring.md`](../../docs/lead-scoring.md).

```bash
npm run lead:score -- --file data/leads/example-lead.json --save
```

Agent: najpierw browser → JSON, potem ten skrypt.
