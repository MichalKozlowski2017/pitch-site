---
description: Pitch-site — landingi OLX, client.json, warianty sekcji
globs:
  - content/**
  - src/components/**
  - src/lib/client-config.ts
  - src/app/**
alwaysApply: true
---

# Pitch Site

- Przed nowym leadem: sprawdź `data/outreach-tracker.csv` (telefon, firma, OLX) — nie pitchuj dwa razy.
- Po wklejeniu linku: **`npm run lead:ingest -- --url <OLX> --phone <tel> --save`** — fetch, zdjęcia, analiza, werdykt (`docs/lead-scoring.md`). Bez `pitch` nie buduj `client.json`.
- Po demo / wysyłce: zaktualizuj tracker (`docs/outreach-tracker.md`).
- Dane klienta i wybór layoutu: tylko `content/client.json` (Zod w `src/lib/client-config.ts`).
- Sekcje renderuj przez `src/components/registry.ts` — nie omijaj registry hardcodem w `page.tsx`.
- Wspólny kontrakt props: `SectionProps` (`{ client }`).
- Opinie: wyłącznie prawdziwe; nigdy nie generuj fake reviews.
- Nowy wygląd = nowy wariant (plik + registry + enum Zod + wpis w `AGENTS.md`).
- Playbooki branż i learnings: `docs/industries/` — aktualizuj po każdym pitchu.
- Demo pitch: `pitch.enabled` + `/pitch`; na produkcji wyłącz badge.
- Przed oddaniem pitcha: sprawdź telefon/CTA, mobile, SEO title/description.
- Przed `client.json`: [`docs/pitch-design-algorithm.md`](docs/pitch-design-algorithm.md) — accent z foto, rotacja wariantów (nie ten sam bento), cennik 3 poz. wyśrodkowany, inny `pricing.pattern` niż poprzedni pitch.
- Brak logo u klienta: zaprojektuj prosty wordmark/monogram (SVG) + dopasuj theme i fonty — patrz `docs/conventions.md` (Branding).
